apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: hygacon-web
  namespace: ynoacamino
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt"
    traefik.ingress.kubernetes.io/redirect-entry-point: https

spec:
  tls:
    - hosts:
        - hygacon-web.ynoacamino.me
      secretName: hygacon-web-tls

  rules:
    - host: hygacon-web.ynoacamino.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hygacon-web
                port:
                  number: 80
