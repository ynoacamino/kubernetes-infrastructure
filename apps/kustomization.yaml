apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - uploader
  - ente

patches:
  - path: patches/statefulset.yaml
    target:
      kind: StatefulSet
      namespace: ynoacamino
