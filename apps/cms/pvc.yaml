apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: cms-data
  namespace: prod

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
