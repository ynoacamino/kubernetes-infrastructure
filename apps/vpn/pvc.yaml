apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vpn-configs
  namespace: ynoacamino
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Mi
