apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: server-ente
  namespace: prod
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/redirect-entry-point: https
spec:
  tls:
    - hosts:
        - ente-kube.ynoacamino.me
      secretName: ente-tls
  rules:
    - host: ente-kube.ynoacamino.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: server-ente
                port:
                  number: 80
