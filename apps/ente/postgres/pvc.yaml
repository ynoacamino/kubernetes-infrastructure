apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-ente-pvc
  namespace: ynoacamino
  labels:
    app: postgres-ente
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
