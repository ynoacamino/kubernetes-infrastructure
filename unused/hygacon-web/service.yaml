apiVersion: v1
kind: Service

metadata:
  name: hygacon-web
  namespace: ynoacamino
  labels:
    app: hygacon-web

spec:
  type: ClusterIP
  selector:
    app: hygacon-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
