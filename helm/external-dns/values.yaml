provider:
  name: cloudflare

domainFilters:
  - ynoacamino.me

txtOwnerId: kubernetes-cluster

policy: sync

env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-api-token
        key: api-token